#include<bits/stdc++.h>
using namespace std;
int main()
{
    int t;
    cin>>t;
    while(t--)
    {
        int n;
        cin>>n;
        vector<int> v;
        for(int i=0;i<n;i++)
        {
            int j;
            cin>>j;
            v.push_back(j);
        }
        std::vector<bool> vector(n,false);
        map<int,int> m;
        for(int j=0;j<v.size();j++)
        {
            
        }
    }
}